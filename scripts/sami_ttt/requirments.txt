# requirements.txt for SAMI Tic-Tac-Toe
# Pin Python version (informational only; pip itself won’t enforce this)
python_version == "3.13"

# Command to download pip install -r requirements.txt
# GUI and serial dependencies
pyserial>=3.5
pygame>=2.5.0
PyQt5>=5.15.10